# miu_v22 云端合并版（云端下载主页实时显示与ZIP角色精准解包修复版）

在 `miu_v21` 基础上，针对从 Google Drive 下载角色卡到 Web 本地后“提示下载成功但在主页未显示”的问题进行了完整修复。

## 本次更新（v22）重点修复
1. **下载角色卡本地目录归属修复 (`src/components/CloudSyncTab.tsx`)**
   - 云端角色卡顶层统一存放在“角色卡”分类中。原先下载时将“角色卡”当作本地子文件夹创建并将卡片放入该子文件夹，导致主页（未分类根目录）无法直接显示新下载的卡片。
   - 现已修复为自动剥离云端根目录前缀“角色卡”，未分类卡片直接保存在本地根目录，下载完成后立即在主页渲染展示。

2. **ZIP 格式角色卡精准解包识别 (`src/lib/cloudDrive.ts`)**
   - 优化 `downloadCloudCharacter` 中的 ZIP 解压与解析逻辑，避免 ZIP 内附带的聊天记录（`chat_*.json`）或快速回复（`*_qr.json`）覆盖真正的角色卡数据。
   - 优先识别 `card.json`、`character.json` 及包含有效角色协议（v2/v3）的 JSON，确保解压后写入数据库的是完整正确的角色卡数据。

3. **设置面板关闭与全局角色列表刷新同步 (`src/App.tsx`)**
   - 在设置面板关闭时自动触发界面数据刷新，确保从设置里的云端同步标签页下载卡片后返回主界面，角色卡列表即刻完成重载并呈现最新卡片。

## 前序已合并修复（v16~v21）
- Google Drive 更新上传 HTTP 动词修正（`PATCH` / `POST`），解决更新已有卡片报错 404/405。
- 绑定 QR 后角色卡类型误判修复，角色卡即使绑定多套 QR 依然保持角色身份。
- 多头像历史（Avatar History）与单卡独立存储，去除多余的空外层文件夹。
- 完整备份 Google Drive 断点续传，支持大文件与上传百分比进度。

## 使用与构建
- 本地运行：`npm install` 后 `npm run dev`
- 构建前端：`npm run build` 或 `node node_modules/vite/bin/vite.js build --configLoader native`
- 同步 Android：`npx cap sync android`
