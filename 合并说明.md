# miu_v23 云端合并版（云端目录体系规范化与子文件夹下载修复版）

在 `miu_v22` 基础上，针对 Google Drive 云端工具区与角色卡分类收纳规则、以及子文件夹角色下载链路进行了深度重构与修复。

## 本次更新（v23）核心逻辑重构
1. **云端多层级目录自动归纳 (`src/lib/cloudDrive.ts`)**
   - **工具区**：所有预设、世界书、美化、快速回复（QR）、脚本/正则等工具卡片，在 Google Drive 云端自动统一按分类创建子文件夹并归入（例如 `AIs_Studio_Cloud_Cards/工具区/预设/`、`工具区/世界书/`、`工具区/美化/`、`工具区/快速回复/`、`工具区/脚本/`），彻底杜绝散落凌乱在云端根目录。
   - **角色卡**：普通角色卡默认收纳于 `AIs_Studio_Cloud_Cards/角色卡/`，若在 App 中归属于用户自定义文件夹（如“原神”、“动漫”），则在云端自动创建对应层级（如 `角色卡/原神/`）。

2. **下载角色卡分类与主页映射修复 (`src/components/CloudSyncTab.tsx`)**
   - 明确 App 主页与云端路径映射：
     - 云端 `角色卡/` 下的文件作为主页未分类卡片下载（`folderId: undefined`），下载后直接在 App 主页展示。
     - 云端 `角色卡/子分类/` 下的文件，下载时自动匹配/新建本地同名子文件夹并将卡片放入。
     - 云端 `工具区/具体分类/` 下的文件，下载时由其自身数据结构直接在 App 对应的工具分类中生效展示。

3. **子文件夹角色下载与多文件配套容错增强 (`src/lib/cloudDrive.ts`)**
   - 修复在云端列表过滤中排除了头像等内部附件（`fileKind: avatar_*`），仅展示独立卡片主体。
   - 增强配套数据智能查找：如果下载的云端卡片为独立头像或独立 JSON，系统会自动在云端查找对应的配套数据或图片，确保角色卡、头像、历史头像完整恢复，不再报错“无效的云端卡片格式”。

## 使用与构建
- 本地运行：`npm install` 后 `npm run dev`
- 构建前端：`npm run build` 或 `node node_modules/vite/bin/vite.js build --configLoader native`
- 同步 Android：`npx cap sync android`
