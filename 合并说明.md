# miu_v21 云端合并版（换头像与绑定QR上传修复版）

在 `miu_v20` 基础上，针对换头像（多头像历史）和绑定快速回复（QR）后无法上传云端的问题进行了彻底排查与修复。

## 本次更新（v21）重点修复
1. **Drive 更新上传 HTTP 方法修正 (`src/lib/driveUpload.ts`)**
   - 修复 `driveMultipartUpload` 在 Web 和 Native 端硬编码 `POST` 导致更新已有文件时 Google Drive 接口返回 404/405 的问题。
   - 保证更新已有角色（`PATCH`）和新建角色（`POST`）均采用正确的 HTTP 动词，并在请求体中同步更新 `mimeType`（支持 PNG 平滑升级为包含替换头像的 ZIP）。
   - 提取 Google Drive API 的详细错误信息到 `statusText`，避免错误信息丢失或为空。

2. **角色卡类型误判修复 (`src/lib/db.ts` & `src/lib/cloudDrive.ts`)**
   - 修复角色卡在绑定 QR（`extensions.tavern_qr_sets` / `quick_replies`）后，`isActualCharacterCard` 因包含 `quick_replies` 误将其判定为工具/脚本，导致 `detectCloudCardType` 将角色误识别为 `qr` 并错误上传到 `工具区/快速回复` 的问题。
   - 完善角色卡识别优先级：只要卡片具备角色名及角色内容（描述/设定/问候语/性格/世界书），无论是否绑定了 QR 均保持角色卡身份。

3. **多头像历史与打包安全过滤 (`src/lib/cloudDrive.ts`)**
   - 优化 `extraAvatars` 过滤与 `createZip` 打包逻辑，确保用户在更换头像后产生的新旧头像历史列表（File / Blob）能够安全、稳定地打入 ZIP 包并上传。

4. **Web 干净版产物**
   - 提供独立纯净的 `miu_v21_web_干净版`，不含任何 Android/原生残留，内置 `vercel.json`，方便直接推送到 GitHub 与 Vercel 部署。

## 使用与构建
- 本地运行：`npm install` 后 `npm run dev`
- 构建前端：`npm run build` 或 `node node_modules/vite/bin/vite.js build --configLoader native`
- 同步 Android：`npx cap sync android`
