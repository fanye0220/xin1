# miu_v24 云端合并版（无头像小机器人兜底修复 + 工具区防重复嵌套 + SillyTavern 预设/QR 识别规范版）

在 `miu_v23` 基础上，针对三大核心体验问题进行了完整修复与深度优化：

## 本次更新（v24）核心修复与改进

1. **无头像卡片下载与显示的小机器人默认头像彻底修复 (`src/lib/avatar.ts`, `src/components/CharacterList.tsx`, `src/components/CharacterDetail.tsx` 等)**
   - **离线内联 SVG Data URI**：`getFallbackAvatar` 生成 100% 离线自包含的 Base64 SVG 小机器人头像，彻底杜绝外网依赖与 Android WebView 跨域加载异常。
   - **全场景 onError 兜底**：在列表卡片（网格、列表、瀑布流）、详情页、多头像查看器、文件夹预览、聊天页面中，图片加载异常或卡片缺失头像时，自动安全回退至小机器人矢量头像，绝不出现“图裂”或空白占位符。
   - **云端下载无头像保底**：从云端下载单独 JSON 或无头像卡片时，自动注入小机器人兜底头像配置并保存，确保下载后在 App 主页及详情页立即可见。

2. **工具区云端分类层级重复嵌套彻底解决 (`src/lib/cloudDrive.ts`)**
   - **防双重嵌套**：修复了上传工具区文件时同时将分类和本地 App 文件夹（如 `世界书`）两度 push 导致云端出现 `工具区/世界书/世界书/xxx.json` 的问题。
   - **标准层级规范**：现在工具区统一规范为 `工具区/世界书/xxx.json`、`工具区/预设/xxx.json`、`工具区/快速回复/xxx.json`、`工具区/美化/xxx.json`、`工具区/脚本/xxx.json`，若有自定义二级子目录则精准为 `工具区/世界书/自定义分类/xxx.json`。

3. **SillyTavern 预设（Preset）与快速回复（QR）精准分类 (`src/lib/db.ts`, `src/lib/cloudDrive.ts`, `src/components/SelectQRModal.tsx`, `src/lib/androidSync.ts`)**
   - **特征签名深度匹配**：兼容 SillyTavern 全系列预设格式（`prompts` 数组、`temperature`、`top_p`、`openai_max_tokens`、`max_context_length`、`system_prompt` 等）以及 SillyTavern QR 集合（`qrList`、`quick_replies`、`tavern_qr_sets`）。
   - **杜绝误判为普通角色卡**：在 `isActualCharacterCard` 与 `getCharacterCategoryPrefix` 中，即便预设或 QR 文件包含 `name` 属性（如 `📱多云熊手机二改版`、`萝卜蛋2.0`），也能精准识别并自动归入“工具区/预设”与“工具区/快速回复”，不再混杂于角色卡主页中。
   - **统一判定源**：`db.ts`、`androidSync.ts`、`cloudDrive.ts`、`CloudSyncTab.tsx` 共享同一套严格规范的工具判定逻辑。

## 使用与构建
- 本地运行：`npm install` 后 `npm run dev`
- 构建前端：`npm run build` 或 `node node_modules/vite/bin/vite.js build --configLoader native`
- 同步 Android：`npx cap sync android`
